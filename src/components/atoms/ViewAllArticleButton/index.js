import React from "react";
import { Button, Container } from "./ViewAllArticleButtonStyles";

export default function ViewAllArticleButton(props) {
  return (
    <Container>
      <Button>Lihat Semua Artikel</Button>
    </Container>
  );
}
